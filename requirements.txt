# ── Web 框架 ────────────────────────────────────────────────
flask>=3.0.0

# ── PDF 解析 ─────────────────────────────────────────────────
pdfplumber>=0.11.0
pymupdf>=1.24.0          # import fitz

# ── OCR fallback（cid 編碼異常時的文字辨識）───────────────────
# 需另外安裝系統 tesseract 主程式與語言包，見 README
pytesseract>=0.3.10
pillow>=10.0.0

# ── Word 文件（樣式對照）────────────────────────────────────
python-docx>=1.1.0

# ── NLP 模型 ─────────────────────────────────────────────────
sentence-transformers>=3.0.0
transformers>=4.40.0
torch>=2.2.0             # CPU 版本即可；GPU 請另行安裝

# ── 模型下載 ─────────────────────────────────────────────────
huggingface_hub>=0.23.0
hf_xet>=0.1.0            # Xet 格式大檔支援

# ── PDF 輸出 ─────────────────────────────────────────────────
weasyprint>=62.0

# ── 資料驗證 ─────────────────────────────────────────────────
# dataclasses 內建，不需額外安裝

# ── 測試 ─────────────────────────────────────────────────────
pytest>=8.0.0
pytest-flask>=1.3.0
